mutation rejectRequest($request: InfoRequest!) {
  rejectRequest(request: $request) {
    isReject
    isPending
    userRequest {
      id
      name
      avatar
    }
    userReceive {
      id
      name
      avatar
    }
  }
}
