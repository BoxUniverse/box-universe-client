mutation acceptRequest($request: InfoRequest!) {
  acceptRequest(request: $request) {
    isAccept
    isPending
    userRequest {
      id
      name
      avatar
    }
    userReceive {
      id
      name
      avatar
    }
  }
}
